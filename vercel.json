{
    "version": 2,
    "builds": [
        {
            "source": "server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "source": "/(.*)",
            "destination": "server.py"
        }
    ]
}